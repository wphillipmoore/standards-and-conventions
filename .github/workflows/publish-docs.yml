name: Publish docs

on:
  push:
    branches: [develop, main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    uses: wphillipmoore/standard-actions/.github/workflows/publish-docs.yml@v1.5
